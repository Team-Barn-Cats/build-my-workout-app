// the component that shows up when someone selects "Lower Body"

const LowerBody = () => {
    return (
        <h2>This is where the list of lower body exercises will be.</h2>
    );
};

export default LowerBody;