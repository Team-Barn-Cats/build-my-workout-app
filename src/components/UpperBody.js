// the component that shows up when someone selects "Upper Body"

const UpperBody = () => {
    return (
        <h2>This is where the list of upper body exercises will be.</h2>
    );
};

export default UpperBody;